import React from "react";

const AboutPage = () => (
    <div>
        <h2>About</h2>

        <p>This app uses React, Redux, React Router, and Material UI to track golf scores and game progress.</p>
    </div>
);

export default AboutPage;
